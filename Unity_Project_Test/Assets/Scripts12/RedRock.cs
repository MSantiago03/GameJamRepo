using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedRock : Rock
{
    // Start is called before the first frame update
    void Start()
    {
        level = 3;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
